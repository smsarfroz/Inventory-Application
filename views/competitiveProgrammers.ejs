<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/styles.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" integrity="sha512-9usAa10IRO0HhonpyAIVpjrylPvoDwiPUiKdWk5t3PyolY1cOd4DSE0Ga+ri4AuTroPR5aQvXU9xC6qOPnzFeg==" crossorigin="anonymous" referrerpolicy="no-referrer" />
    <title><%= title %></title>
</head>
<body>
    <%- include('navbar') %>
        <h2>Competitive Programmers</h2>
        <form action="/new" method="GET">
            <div class="buttonContainer"><button class="addButton">Add Programmer</button></div>
        </form>

        <div class="programmersContainer">
            <%programmers.map(user => {%>
                <div class="programmer">
                    <div class="editdeleteContainer">
                        <form action="/<%= user.programmer_id %>/update" method="GET">
                            <button type="submit"><i class="fas fa-sync"></i></button>
                        </form>
                        <form action="/<%= user.programmer_id %>/delete" method="POST">
                            <button type="submit"><i class="fas fa-trash-alt"></i></button>
                        </form>
                    </div>
                    <p class="programmerName"><%=user.programmer%></p>
                    <img src="<%= user.imageurl %>" alt="" class="profileImage">
                    <div class="tagContainer">
                        <div class="contributionTag">
                            <h4>Contribution</h4>
                            <%user.contributionArray.map(object => {%>
                               <button class="tagButton"><%= object.contribution %></button>
                            <%});%>
                        </div>
                        <div class="ratingTag">
                            <h4>Max Rating</h4>
                            <button class="tagButton"><%= user.maxrating %></button>
                        </div>
                    </div>
                </div>
            <%});%>
        </div>
    <%- include('footer') %>
</body>
</html>