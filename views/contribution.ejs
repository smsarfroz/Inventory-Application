<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/styles.css">
    <title><%= title %></title>
</head>
<body>
    <%- include('navbar') %>
        <h2>Contribution</h2>
        <form action="/contribution/new" method="GET">
            <button>Add contribution</button>
        </form>

        <%contributions.map(contribution => {%>
            <div>
                <%=contribution.contribution%>
                <form action="/contribution/<%= contribution.contribution_id %>/update" method="GET">
                    <button type="submit">update</button>
                </form>
                <form action="/contribution/<%= contribution.contribution_id %>/delete" method="POST" style="display: inline;">
                    <button type="submit">delete</button>
                </form>
            </div>
        <%});%>
    <%- include('footer') %>
</body>
</html>